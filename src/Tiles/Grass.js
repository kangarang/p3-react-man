import React from 'react';

export default (props) => {
    const styles = {
        grass: {
            height: "100%",
            width: "100%",
        },

    }
    return <div style={styles.grass}></div>
}
